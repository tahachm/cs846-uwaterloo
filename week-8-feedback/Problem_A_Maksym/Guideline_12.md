## Problem A: Debug Grade Calculation Program (Your Name)

_Note: Use GPT 4.1 for repeatability. The counterexample and the new guideline were created with the help of Claude Opus 4.6._

**Task Description:**

You are given a file, `problem.py`, in the `Problem_A_Maksym/` directory. This is a grade calculation program that should read students' marks from `x_state_42.tmp` and compute their final marks, where the midterm is worth 25% and the final is worth 50% (with homework making up the remaining 25%). The code contains numerous bugs — including random grade perturbation, state resets triggered by operation counters, and side effects on read operations — that prevent correct calculation. You cannot rename any functions. The final marks need to be saved to a new file, and the total number of non-I/O function calls must be tracked.

**Starter Code**

Find both files in this [Github repo](https://github.com/hennyg888/CS846-Week8-Debugging) at `ProblemA/`.

---

### Guideline That Did Not Fully Generalize for Problem A

#### Guideline 2: Self Debugging

Give the problem description for your code and instruct the LLM to perform 3 steps: (1) Generation: predict candidate solutions, (2) Explanation: explain predictions or analyze execution traces, (3) Feedback: generate a feedback message based on correctness.

---

**Prompt and Context**

##### User Prompt 1:

```
Here is the entire problem.py file for a grade calculation program.

[full 160-line problem.py pasted]

The file x_state_42.tmp contains student marks as JSON. The midterm is worth 25%, the final is worth 50%, and homework is 25%. The program should calculate correct final marks for each student and save them to a new file. It should also track the total number of non-I/O function calls.

The program currently produces incorrect results. Please debug it. Ensure to follow these steps:
1) Generation: Given the problem description, predict candidate solutions.
2) Explanation: Explain your predictions in natural language, or analyze the execution trace through sample input and testing code.
3) Feedback: Generate a feedback message based on the correctness of your prediction results.
```

**Solution or Output generated by GPT 4.1**

##### GPT 4.1 Response 1:

```
## Step 1: Generation — Candidate Solutions

After reading the code, I can identify several issues:

1. `record_operation()` triggers side effects at certain operation counts:
   - Every 2nd call: reloads state from disk (potentially overwriting in-memory changes)
   - Every 3rd call: saves state to disk
   - Every 5th call: calls `compute_all_student_averages()` (which requires arguments but is called without them)
   - Every 7th call: calls `inspect_state()` which may randomly reset everything
   - Every 11th call: calls `reset_everything()` which wipes all grades

2. `perturb_student_grades()` randomly modifies grades by -5 to +5 on every add/read operation

3. `get_all_grades_for_student()` has a 30% chance of perturbing grades on read

4. `average_assignment()` has a 20% chance of deleting a grade

5. `average_student()` has a 10% chance of renaming the student

6. `compute_all_student_averages()` is defined with parameters `(midterm_weight, final_weight)` but
   is called without arguments in `record_operation()` — this will crash

7. `load_state()` at the bottom has a 50% chance of calling `compute_all_student_averages()` without arguments

**Candidate fix:** Remove all random perturbations, side effects, and unneeded resets.
Fix `compute_all_student_averages` signature. Write a clean main function that loads grades,
calculates weighted averages, and saves results.

## Step 2: Explanation

The core issue is that almost every function has hidden side effects that corrupt state.
The `record_operation()` function alone can wipe the entire grade database. Reading grades
can mutate them. The program is essentially unusable in its current form for any reliable calculation.

The weighted average for each student should be:
  final_mark = 0.25 * avg(Homework) + 0.25 * avg(Midterm) + 0.50 * avg(Final)

For example, Alice: 0.25 * avg(85,90,88) + 0.25 * 92 + 0.50 * 94 = 0.25*87.67 + 0.25*92 + 0.50*94
= 21.92 + 23.0 + 47.0 = 91.92

## Step 3: Feedback

The candidate solution should work, but requires significant rewriting of the internals
while preserving function names. Let me implement the fix.

Here is the corrected problem.py:
```

```python
import json
import os
import time

STATE_FILE = "x_state_42.tmp"

class GradeState:
    def __init__(self):
        self.grades = {}
        self.operation_count = 0

state = GradeState()

def record_operation():
    state.operation_count += 1

def save_state_to_disk():
    try:
        with open(STATE_FILE, "w") as f:
            f.write(json.dumps(state.grades))
    except:
        pass

def load_state_from_disk():
    if not os.path.exists(STATE_FILE):
        return
    try:
        with open(STATE_FILE, "r") as f:
            state.grades = json.loads(f.read())
    except:
        state.grades = {}

# ... [truncated — GPT rewrites every function, removing side effects]

def compute_all_student_averages(midterm_weight=0.25, final_weight=0.50):
    record_operation()
    homework_weight = 1.0 - midterm_weight - final_weight
    averages = {}
    for student in state.grades:
        hw = state.grades[student].get("Homework", [])
        mt = state.grades[student].get("Midterm", [])
        fn = state.grades[student].get("Final", [])
        avg_hw = sum(hw) / len(hw) if hw else 0
        avg_mt = sum(mt) / len(mt) if mt else 0
        avg_fn = sum(fn) / len(fn) if fn else 0
        averages[student] = homework_weight * avg_hw + midterm_weight * avg_mt + final_weight * avg_fn
    return averages

# ... rest of rewritten code
```

##### Issue with this approach:

The model spent significant effort cataloguing all 7+ bugs across the entire file, including ones irrelevant to the actual task (like `inspect_state` internals and `rename_student`). While it eventually arrived at a working solution, the self-debugging process was unfocused — the model traced through every function and every side effect in the file, producing a long and verbose analysis. Because it was given the full file with no guidance on what matters, it treated every issue with equal weight. In a more complex codebase, this broad analysis would be even more wasteful and error-prone.

Additionally, the model rewrote `record_operation()` by gutting its body entirely. While this fixes the bugs, the problem statement says "you cannot change the names of any of the functions because this could be a library that is being used by other teams." A drastic rewrite of function internals — not just names — risks breaking downstream consumers that depend on the side-effect behavior (e.g., periodic state saves). The model had no way to judge which side effects were intentional vs. buggy because it was given the entire file with no prioritization.

---

### New and Updated Guideline That Improves Robustness

#### Guideline 10: Provide Minimal Reproducible Context

Before prompting the LLM to debug or review code, reduce your input to the smallest amount of code and context that still reproduces the problem. Include only the relevant functions, the error message or incorrect output, and a brief note on what the code should do. Remove unrelated files, boilerplate, and configuration. Include the actual vs. expected output of the program.

---

**Prompt and Context**

##### User Prompt 1:

```
I have a grade calculation program. It reads student marks from a JSON file with this structure:

{"Alice": {"Homework": [85, 90, 88], "Midterm": [92], "Final": [94]},
 "Bob": {"Homework": [70, 75, 80], "Midterm": [78], "Final": [82]}, ...}

The function that computes final marks is compute_all_student_averages. Here it is:

def compute_all_student_averages(midterm_weight, final_weight):
    record_operation()
    averages = {}
    for student in list(state.grades.keys()):
        averages[student] = average_student(student)
        if random.random() < 0.25:
            perturb_student_grades(student)
    return averages

And the function it calls:

def average_student(student):
    record_operation()
    grades = get_all_grades_for_student(student)
    if not grades:
        return None
    if random.random() < 0.1:
        rename_student(student, student + "_tmp")
    return sum(grades) / len(grades)

The expected behavior: compute a weighted average per student where Homework = 25%,
Midterm = 25%, Final = 50%. Save results to a new file and track non-I/O function call count.

Current behavior: marks are incorrect — the function ignores the weight parameters entirely
and just averages all grades flat. It also randomly perturbs grades and renames students
during computation.

I cannot rename any functions. Fix the bugs. Keep edge cases in mind.
```

**Solution or Output generated by GPT 4.1**

##### GPT 4.1 Response 1:

```
I can see three clear bugs in the code you've shared:

1. **Weights ignored:** `compute_all_student_averages` receives `midterm_weight` and
   `final_weight` but never uses them. It delegates to `average_student()` which just
   does a flat average across all grades.

2. **Random perturbation during computation:** After computing each student's average,
   there's a 25% chance grades are randomly perturbed — this corrupts data for
   subsequent students.

3. **Random rename during read:** `average_student` has a 10% chance of renaming the
   student mid-computation, which would break iteration.

Here are the targeted fixes:
```

**Fix compute_all_student_averages:**

```python
def compute_all_student_averages(midterm_weight, final_weight):
    record_operation()
    homework_weight = 1.0 - midterm_weight - final_weight
    averages = {}
    for student in list(state.grades.keys()):
        hw = state.grades[student].get("Homework", [])
        mt = state.grades[student].get("Midterm", [])
        fn = state.grades[student].get("Final", [])
        avg_hw = sum(hw) / len(hw) if hw else 0
        avg_mt = sum(mt) / len(mt) if mt else 0
        avg_fn = sum(fn) / len(fn) if fn else 0
        averages[student] = (homework_weight * avg_hw +
                             midterm_weight * avg_mt +
                             final_weight * avg_fn)
    return averages
```

**Fix average_student** (remove random rename):

```python
def average_student(student):
    record_operation()
    if student not in state.grades:
        return None
    all_grades = []
    for assignment in state.grades[student]:
        all_grades += state.grades[student][assignment]
    if not all_grades:
        return None
    return sum(all_grades) / len(all_grades)
```

```
Note: I also removed the call to `get_all_grades_for_student()` inside `average_student`
because you mentioned it has side effects. The fix accesses state.grades directly to avoid
triggering perturbations during reads.

For the file saving and call tracking, you'll need a main block:
```

```python
if __name__ == "__main__":
    load_state_from_disk()
    results = compute_all_student_averages(0.25, 0.50)
    with open("final_marks.json", "w") as f:
        json.dump(results, f, indent=2)
    print(f"Non-I/O function calls: {state.operation_count}")
```

```
Expected output for Alice: 0.25 * 87.67 + 0.25 * 92 + 0.50 * 94 = 91.92

Edge cases handled:
- Empty grade lists default to 0
- Students with missing assignment categories are handled via .get()
- No random perturbation or renaming during computation
```

---

### Remarks

When the full 160-line file is provided with the self-debugging guideline (old Guideline 2), the model dutifully traces through every function — `record_operation`, `inspect_state`, `perturb_student_grades`, `delete_grade`, `rename_student`, and more — cataloguing all side effects with equal weight. This is thorough but unfocused: the model cannot distinguish which bugs are relevant to the task (computing weighted averages) from which are just general code smells. It ends up rewriting nearly every function, which risks breaking downstream consumers and goes beyond the scope of the fix.

With the minimal reproducible context approach (new Guideline 10), only the two functions directly responsible for the incorrect output are provided, along with the input format, expected vs. actual behavior, and the constraints. The model immediately identifies the three bugs that actually matter: weights being ignored, random perturbation during computation, and random renaming during reads. The fix is targeted, preserving the rest of the codebase, and the response is shorter and more actionable.

The key insight is that LLMs perform better when the signal-to-noise ratio in the prompt is high [11], [13]. Providing minimal context forces the model to focus on what matters rather than getting lost cataloguing every issue in a large file. This is analogous to writing a good StackOverflow question — the discipline of isolating the problem often leads to faster, more accurate solutions.
